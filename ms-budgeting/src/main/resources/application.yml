# BASE / DEV (default)
server:
  port: 8084                  # porta do ms-budgeting
  servlet:
    context-path: /budgeting   # base path do serviço
  shutdown: graceful           # encerra com segurança
  compression:
    enabled: true              # compressão HTTP
    mime-types: >
      text/html,text/xml,text/plain,text/css,
      text/javascript,application/javascript,
      application/json,application/xml
    min-response-size: 1024    # só comprime acima disso
  forward-headers-strategy: framework  # lê X-Forwarded-* (proxy/gateway)

spring:
  application:
    name: ms-budgeting         # nome do serviço

  main:
    allow-bean-definition-overriding: false
    keep-alive: true
    web-application-type: servlet
    threads:
      virtual:
        enabled: true          # threads virtuais (Java 21+/Boot 4)

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5436/ecofy_budgeting} # dev local
    username: ${DB_USER:ecofy_budgeting}
    password: ${DB_PASS:ecofy_budgeting}
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: MsBudgetingHikariPool
      maximum-pool-size: ${DB_POOL_MAX:10}
      minimum-idle: ${DB_POOL_MIN:2}
      idle-timeout: 300000
      max-lifetime: 1800000
      connection-timeout: 30000
      leak-detection-threshold: 0   # 0 desliga

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate        # não cria tabelas (usa Flyway)
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          time_zone: UTC
    show-sql: false

  flyway:
    enabled: true
    baseline-on-migrate: true   # baseline automático (evite em prod)
    default-schema: ${DB_SCHEMA:ecofy_budgeting}
    locations: classpath:db/migration

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:19092} # dev local
    client-id: ms-budgeting
    properties:
      security.protocol: ${KAFKA_SECURITY_PROTOCOL:PLAINTEXT}      # dev (prod usa SSL/SASL_SSL)

    consumer:
      group-id: ${KAFKA_CONSUMER_GROUP_ID:${spring.application.name}}
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: earliest
      max-poll-records: ${KAFKA_MAX_POLL_RECORDS:10}
      properties:
        spring.json.trusted.packages: ${KAFKA_TRUSTED_PACKAGES:br.com.ecofy.ms_budgeting.adapters.in.kafka.dto}
        spring.json.use.type.headers: false

    producer:
      acks: all
      retries: 3
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        enable.idempotence: true  # evita duplicar publish

    listener:
      ack-mode: record            # commit por mensagem

  jackson:
    time-zone: UTC

  sql:
    init:
      mode: never

  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${JWT_JWKS_URI:http://localhost:8081/auth/.well-known/jwks.json} # dev local

ecofy:
  budgeting:
    topics:
      categorized-transaction: ${BGT_TOPIC_TX_CATEGORIZED:eco.transaction.categorized}
      budget-alert: ${BGT_TOPIC_BUDGET_ALERT:eco.budget.alert}

    idempotency:
      ttl: ${BGT_IDEMPOTENCY_TTL:PT24H}

    alerts:
      warning-threshold-pct: ${BGT_ALERT_WARNING_PCT:80}
      critical-threshold-pct: ${BGT_ALERT_CRITICAL_PCT:95}
      publish-on-every-update: ${BGT_ALERT_PUBLISH_ON_EVERY_UPDATE:false}

    schedulers:
      recalculation-enabled: ${BGT_SCHED_RECALC_ENABLED:true}
      cleanup-enabled: ${BGT_SCHED_CLEANUP_ENABLED:false}
      cleanup-retention-days: ${BGT_CLEANUP_RETENTION_DAYS:90}
      cleanup-cron: ${BGT_CLEANUP_CRON:0 0 3 * * *}
      recalculate-cron: ${BGT_RECALC_CRON:0 0/15 * * * *}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when_authorized
  health:
    db:
      enabled: true
    kafka:
      enabled: true

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
    display-request-duration: true

logging:
  level:
    root: INFO
    br.com.ecofy.ms_budgeting: DEBUG
    org.springframework.security: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE


# PROFILE: test
---
spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: ${DB_URL_TEST:jdbc:postgresql://localhost:5433/ecofy_budgeting_test}
    username: ${DB_USER_TEST:ecofy_budgeting_test}
    password: ${DB_PASS_TEST:}   # sem default fraco

  jpa:
    hibernate:
      ddl-auto: create-drop       # cria e apaga no fim
    show-sql: false
    open-in-view: false

  flyway:
    enabled: false

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS_TEST:localhost:19092}
    consumer:
      group-id: ${KAFKA_CONSUMER_GROUP_ID_TEST:ms-budgeting-test}

logging:
  level:
    root: WARN
    br.com.ecofy.ms_budgeting: INFO

ecofy:
  budgeting:
    idempotency:
      ttl: PT1M
    alerts:
      warning-threshold-pct: 80
      critical-threshold-pct: 95
      publish-on-every-update: false


# PROFILE: prod
---
spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: ${DB_URL}               # sem default local
    username: ${DB_USER}
    password: ${DB_PASS}
    hikari:
      pool-name: MsBudgetingHikariPool
      maximum-pool-size: ${DB_POOL_MAX:20}
      minimum-idle: ${DB_POOL_MIN:5}
      idle-timeout: 300000
      max-lifetime: 1800000
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false

  flyway:
    enabled: true
    baseline-on-migrate: ${FLYWAY_BASELINE_ON_MIGRATE:false} # default false
    default-schema: ${DB_SCHEMA:ecofy_budgeting}
    locations: classpath:db/migration

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}            # sem default local
    properties:
      security.protocol: ${KAFKA_SECURITY_PROTOCOL:SASL_SSL} # default seguro

  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${JWT_JWKS_URI}                       # obrigatório

logging:
  level:
    root: INFO
    br.com.ecofy.ms_budgeting: INFO
    org.hibernate.SQL: WARN
    org.springframework.security: WARN
    org.springframework.web: WARN

ecofy:
  budgeting:
    topics:
      categorized-transaction: ${BGT_TOPIC_TX_CATEGORIZED:eco.transaction.categorized}
      budget-alert: ${BGT_TOPIC_BUDGET_ALERT:eco.budget.alert}

    idempotency:
      ttl: ${BGT_IDEMPOTENCY_TTL:PT24H}

    alerts:
      warning-threshold-pct: ${BGT_ALERT_WARNING_PCT:80}
      critical-threshold-pct: ${BGT_ALERT_CRITICAL_PCT:95}
      publish-on-every-update: ${BGT_ALERT_PUBLISH_ON_EVERY_UPDATE:false}
